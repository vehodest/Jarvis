<Window x:Class="PriceMonitor.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:PriceMonitor"
		xmlns:dataTypes="clr-namespace:Entity.DataTypes;assembly=Entity"
		mc:Ignorable="d"
		Title="Util" Height="600" Width="800">
	<Grid>
		<Border Margin="10">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="3*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<TreeView Grid.Column="0" Grid.Row="0" Grid.RowSpan="3"
					ItemsSource="{Binding Path=MenuItems}"
					SelectedItemChanged="TreeView_OnSelectedItemChanged"
					ScrollViewer.VerticalScrollBarVisibility="Visible">
					<TreeView.ItemTemplate>
						<HierarchicalDataTemplate DataType="{x:Type dataTypes:ObjectsChain}" ItemsSource="{Binding SubObjects}">
							<WrapPanel>
								<TextBlock Text="{Binding Object.Name}" />
							</WrapPanel>
						</HierarchicalDataTemplate>
					</TreeView.ItemTemplate>
				</TreeView>

				<Grid Row="0" Column="1">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition/>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition/>
					</Grid.ColumnDefinitions>
					<Label Content="First Region: " Grid.Column="0"/>
					<ComboBox Grid.Column="1" ItemsSource="{Binding Path=RegionList}" SelectedItem="{Binding Path=FirstRegion}" DisplayMemberPath="Name"/>
					<Label Content="Second Region: " Grid.Column="2"/>
					<ComboBox Grid.Column="3" ItemsSource="{Binding Path=RegionList}" SelectedItem="{Binding Path=SecondRegion}" DisplayMemberPath="Name"/>
				</Grid>

				<Button Content="Check it!" Grid.Row="2" Grid.Column="1" Width="80" Height="40" HorizontalAlignment="Right"
					Command="{Binding Path=CheckPriceCmd}">
				</Button>
			</Grid>
		</Border>
	</Grid>
</Window>
